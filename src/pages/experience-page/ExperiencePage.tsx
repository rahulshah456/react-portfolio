import React from "react";
import PageDemo from "../../components/page-demo/PageDemo";

const ExperiencePage = (props: any) => {
  return (
    <PageDemo pageId={props.id} header="Experience" subHeader="Corporate" />
  );
};

export default ExperiencePage;
