import React from "react";
import PageDemo from "../../components/page-demo/PageDemo";

const SkillsPage = (props: any) => {
  return <PageDemo pageId={props.id} header="Skills" subHeader="Expertise" />;
};

export default SkillsPage;
